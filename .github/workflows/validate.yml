name: validate

# Runs the private stalker-manager pipeline against this public mod.
# The pipeline code lives in the private stalker-dev repo, checked out here with a
# read-only deploy key (secret PIPELINE_SSH_KEY) onto an ephemeral runner. Nothing
# about the pipeline's internals is stored in or printed from this repo beyond the
# validator's own findings about this mod's code.

on:
  push:
  workflow_dispatch:

# Least privilege: the job only reads this repo. The private pipeline arrives via the deploy key,
# and the check is the run's own status -- no GITHUB_TOKEN writes are needed.
permissions:
  contents: read

# One validation per branch: a newer push cancels an in-flight run of the same ref.
concurrency:
  group: validate-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: windows-latest
    steps:
      - name: Checkout mod
        uses: actions/checkout@v7
        with:
          path: xlibs

      # This mod IS xlibs, so no sibling-xlibs checkout (test-load.py skips the xlibs dep for it).

      - name: Checkout pipeline (private stalker-dev)
        uses: actions/checkout@v7
        with:
          repository: damiansirbu-stalker/stalker-dev
          ssh-key: ${{ secrets.PIPELINE_SSH_KEY }}
          path: pipeline
          # Only the validator tree -- keeps the rest of the private repo (other mods' tooling,
          # docs, the engine store) off the runner. With sparse-checkout set, checkout@v4 skips its
          # upfront `git lfs fetch` (which would pull EVERY LFS object on the ref, incl. the ~40
          # engine-variant binaries) and lazily smudges only in-cone LFS -- i.e. stalker-manager/bin/*.exe.
          sparse-checkout: stalker-manager
          lfs: true

      - name: Checkout redlint (prose lane; private)
        uses: actions/checkout@v7
        with:
          # The prose reviewer. redlint_docs.py resolves it as a sibling of the pipeline checkout
          # ($GITHUB_WORKSPACE/redlint), so the load layout matches local; without it the redlint
          # lane degrades to "skipped".
          repository: damiansirbu-org/redlint
          ssh-key: ${{ secrets.REDLINT_SSH_KEY }}
          path: redlint

      - name: Cache pipeline env
        uses: actions/cache@v6
        with:
          path: pipeline/stalker-manager/env
          key: mm-env-${{ hashFiles('pipeline/stalker-manager/requirements.txt') }}

      - name: Setup pipeline env
        shell: bash
        run: |
          if [ ! -d pipeline/stalker-manager/env/stalker-manager ]; then
            bash pipeline/stalker-manager/stalker-manager.sh setup
          fi

      - name: Install uv (redlint venv builder)
        uses: astral-sh/setup-uv@v10.0.1
        with:
          enable-cache: true

      - name: Build redlint venv
        shell: bash
        # uv sync + the spaCy model wheel -> redlint/.venv/Scripts/redlint.exe, which
        # redlint_docs.py:_redlint_exe() looks for. Cached uv downloads keep reruns fast.
        run: bash redlint/redlint.sh setup

      - name: Point pipeline at this checkout
        shell: bash
        run: |
          ws="${GITHUB_WORKSPACE//\\//}"
          {
            echo "repo_path: \"$ws\""
            echo "mods_path: \"$ws\""
            echo "dev_mods:"
            echo "  - name: \"xlibs\""
            echo "    repo: \"$ws/xlibs\""
          } > pipeline/stalker-manager/dev.yaml
          cat pipeline/stalker-manager/dev.yaml

      - name: Validate
        shell: bash
        run: |
          # Grade-gate: validate-dev-mods exits nonzero on any gating finding (prose is unscored and
          # never gates). Run without -e so the summary still publishes on a red run, then re-raise the
          # code so the check goes red. Prose findings surface as a non-gating warning annotation.
          set +e
          bash pipeline/stalker-manager/stalker-manager.sh validate-dev-mods xlibs --full --emit out
          rc=$?
          [ -f out/summary.md ] && cat out/summary.md >> "$GITHUB_STEP_SUMMARY"
          if grep -qE '[0-9]+ findings /' out/summary.md 2>/dev/null; then
            echo "::warning::redlint prose findings present (advisory, not gated); see the run summary"
          fi
          exit $rc

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: out

  # Publish the Project Health page (out/index.html + badge.json) to GitHub Pages.
  deploy-pages:
    needs: validate
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
