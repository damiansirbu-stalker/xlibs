xlibs: Разделяемая библиотека утилит для моддинга STALKER Anomaly, автор Damian
Версия: 1.8.1
GitHub: https://github.com/damiansirbu-stalker/xlibs
Список изменений: https://github.com/damiansirbu-stalker/xlibs/blob/main/doc/changelog
English: https://github.com/damiansirbu-stalker/xlibs/blob/main/doc/readme.txt
Баги, предложения: https://github.com/damiansirbu-stalker/xlibs/issues

xlibs -- набор инструментов моддера, покрывающий всё, что обычно нужно модам Anomaly: запросы сущностей, операции с отрядами, логика умных укрытий, работа с тайниками, логирование, профилирование, системы событий и структуры данных.

Дизайн API основан на реверс-инжиниринге движка X-Ray и внутренностей Anomaly, с перекрёстной проверкой по паттернам лучших моддеров как европейской, так и русской традиции моддинга STALKER. Каждая функция оборачивает причуды движка, защищает от nil и обрабатывает крайние случаи, которые иначе каждому моду пришлось бы решать самостоятельно.

Чистый Lua где возможно. Без зависимости от движка, если не необходимо. Без центрального загрузчика -- движок автозагружает скрипты при первом обращении. Просто вызовите xlog.get_logger() или xsquad.find_squads() -- и всё работает.

Возможности:

Сущности и мир:
  xcreature    Идентификация существ, проверки типов, переведённые имена, денежные примитивы
  xsquad       Поиск отрядов, скриптовое управление, освобождение, погоня, итерация
  xsmart       Запросы умных укрытий, определение фракции, вместимость, прибытие, захват, распределение работ
  xlevel       Запросы уровней/карт, игровое время, названия локаций, валидация вершин
  xobject      Разрешение серверных и онлайн игровых объектов из любого типа входа
  xinventory   Категоризация предметов (тиры боеприпасов на NPC), семантика слотов, конфиг боеприпасов, жизненный цикл предметов, примитивы LTX-политики (load + classify + iterate surplus)
  xstash       Обнаружение тайников, ограбление, пополнение, фильтрация предметов
  xdata        Таблицы нескриптуемых NPC/отрядов (торговцы, механики, сюжетные персонажи)
  xconst       Константы-часовые движка X-Ray (невалидный ID сущности, невалидный ID вершины уровня)

Структуры данных:
  xtable       Подсчёт, перемешивание, сортировка, объединение и вычитание множеств
  xttltable    TTL-хранилище ключ-значение, скользящий оконный счётчик, токен-бакет, FIFO-кэш
  xmath        Случайная выборка и частичное перемешивание
  xslice       Покадровая итерация массивов
  xstring      Строковая интерполяция с плейсхолдерами {key}
  xtime        Аккумулятор игрового времени в секундах (обёртка над game_time())

Диагностика:
  xlog         Буферизованное файловое логирование с управлением сессиями и ротацией
  xprofiler    Профилирование кода с точностью до микросекунд через profile_timer движка
  xtrace       Генерация ID трассировки для отслеживания запросов между модулями
  xinspect     Глубокая инспекция таблиц и userdata, идентификация типов движка

Эффекты:
  xpp          Обёртка пост-процесс эффекторов (аллокатор слотов, движковое сглаживание, API хэндлов; 35 проверенных путей .ppe)
  xsound       Обёртка зацикленных звуков (API хэндлов, Lua-сторонний lerp громкости, ленивый тик actor_on_update)

Интеграция:
  xbus         Pub/sub шина событий (прямая доставка, ошибки остаются видимыми)
  xevent       Рантаймовый хукинг функций для синтетических коллбэков
  xpda         КПК-сообщения и маркеры карты (отряды и сущности)
  xmcm         Комплект конфигурации MCM (предзаполненная таблица, геттер, загрузчик)
  xchange      Реестр миграций сохранений в стиле Liquibase; каждый changeset выполняется один раз на сейв

Требования:
Anomaly 1.5.3

Установка (MO2):
1. Установите xlibs
2. Порядок загрузки не имеет значения (скрипты автозагружаются движком по требованию)

Удаление (MO2):
Отключите или удалите в MO2. Любой мод, зависящий от xlibs, перестанет работать.

Конфигурация:
Настройка не требуется. xlibs -- пассивная библиотека, загружаемая по требованию другими модами.

Совместимость:
Работает на моддированных exe themrdemonized 2025.9.10 или новее, либо AOEngine v0.55 или новее.
Чистая библиотека. Не модифицирует базовые скрипты и не добавляет собственного игрового поведения. xlog при старте игры регистрирует коллбэки сброса буфера при сохранении и смене уровня, а также периодический таймер сброса; всё остальное остаётся неактивным, пока мод это не вызовет. Совместима со всем, включая GAMMA.

Разработка:
Написано на основе исходного кода X-Ray Monolith, исходного кода моддед-экзешников Demonized и распакованных gamedata Anomaly 1.5.3.
Паттерны кода и использование движка валидированы по работам авторитетных GAMMA-моддеров (Demonized, Vintar0, RavenAscendant, xcvb).
Код валидируется в реальном времени многоступенчатым конвейером: luacheck, selene, tree-sitter AST анализ, контрактные правила, разрешение кросс-файловых зависимостей, анализ цикломатической сложности, обнаружение паттернов крашей и уязвимостей, интеграционное тестирование lua54 со стабами движка X-Ray, сканирование секретов gitleaks.
Полный отчёт в doc/test-report.log.

Часто задаваемые вопросы:

Нужны ли моддированные exe?
  Да. xlibs требует моддированные exe от themrdemonized (2025.9.10 или новее) или AOEngine (v0.55 или новее). Ванильный Anomaly не предоставляет API, на которые он опирается.

Авторы:
Altogolik -- поддержка, идеи, исходные материалы

Использование и лицензия:
  Вызов функций xlibs из вашего мода: предусмотренное использование, без ограничений.
  Модпаки: разрешены и приветствуются. Сохраняйте файлы readme и лицензии.
  Аддоны, патчи, интеграции: разрешены. Укажите "xlibs by Damian Sirbu" заметно на странице вашего мода.
  Воспроизведение реализации в другом программном обеспечении: запрещено, даже с указанием авторства.
  Полная лицензия в файле LICENSE и на GitHub.

Сообщения о проблемах и предложения
Создайте баг-репорт или предложение на https://github.com/damiansirbu-stalker/xlibs/issues/new/choose.
Также обсуждается на Discord-серверах GAMMA, EFP, Anomaly и Zona.

Перед публикацией прочитайте этот readme.

Приложите:
- Точные шаги воспроизведения, с новой игры или с названного сохранения, ожидаемый и фактический результат.
- xray.log, сборку движка, список модов и порядок загрузки.
- Опишите поведение. Среди сотен модов и переопределений только лог показывает, причастен ли этот мод и в чём причина.
